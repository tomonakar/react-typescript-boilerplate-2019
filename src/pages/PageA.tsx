import * as React from 'react'

const PageA = () => {
  return (
    <div style={container}>
      <span>PageA</span>
    </div>
  )
}

export default PageA

const container = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
}
